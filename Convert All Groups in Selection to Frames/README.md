# Convert All Groups in Selection to Frames

選択範囲に含まれるすべてのGroupをFrameに変換するFigmaプラグインです。

## 使い方

1. Figmaで変換したい範囲を選択します。
2. このプラグインを実行します。
3. 選択ノード配下にあるすべてのGroupが、階層を保ったままFrameに変換されます。

変換後のFrame名はすべて `Frame` になります。
